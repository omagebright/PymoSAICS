name: test

on:
  push:
  pull_request:

jobs:
  core:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Compile package
        run: python -m compileall -q pymosaics
      - name: Run unit tests
        run: python -m unittest discover -v
      - name: Build Plugin Manager archive
        run: python scripts/build_release.py
      - name: Inspect archive
        run: python -c "from pathlib import Path; from pymosaics import __version__; import zipfile; archive = Path('dist') / ('PymoSAICS-' + __version__ + '.zip'); print(*zipfile.ZipFile(archive).namelist(), sep='\n')"
